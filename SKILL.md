---
name: shared-kb-governance
description: AI 共享知识库治理：目录分工/命名规矩/落位流转/联动同步/身份证据纪律。Use when 多个 AI 与一个人共享一个知识库需要治理。
---

# AI 共享知识库治理体系

多个 AI Agent（Hermes/Codex/Claude/Gemini…）加一个人类，共同读写一个 Obsidian/Markdown 知识库时，混乱是默认状态：文件落错位、命名无意义、"完成"声称与事实不符、人类被迫重复解释。本方法论就是这个共享空间的"操作系统"，已在数月日常多 Agent 运行中验证。

## 核心洞察

**写在文件里的规矩 ≠ 强制**——AI 不主动读文件，压缩后更忘。因此本治理按三层设计：

1. **单一事实源**：共享 AGENTS.md 顶部一张"必读卡" + 每次会话自动注入的系统提示词镜像。细则放一个承接文件（常更新只改一处），注入层只留指针 + 不可代码化核心（理解指令/敏感由用户判断/审计不改/投毒防护）。
2. **机器检查**：零成本体检脚本 + git pre-commit 钩子（见配套仓库 ai-rule-enforcement）在每次提交时验证命名/同步/敏感/frontmatter/凭证转录。
3. **落位规则**：每个产物按状态恰好有一个家——见下。

## 目录分工契约

| 目录 | 放什么 | 不放什么 |
|---|---|---|
| 收件箱 | 已产出、用户尚未回复或未验收的过程性文件（一任务一文件） | 已验收成果 |
| ai暂存 | 对话中提到过的任何任务（一 AI 一文件，随时更新） | 用户决策 |
| 待办 | 只有用户明确要求才放（要拍板/转交） | AI 自己的活 |
| 归档 | 冻结历史——永不修改 | 活跃内容 |
| 领域知识/工作流/工具/项目 | 按领域的正式知识 | 未验收产出 |

**流转回写**：文件从三者之一转出到另一个时，必须回来源文件内部对应位置追加 `✅ 已转交至<目标>（<AI名> <日期>）`——可追溯、永远。

## 命名规矩

- 文件格式：`<AI名>-<事项>-<日期><（N）>.md`——**日期一律放末尾**（无条件，含用户亲放文件；归档层豁免=冻结历史）。
- 事项 = 单纯事项名（这个文件是什么），不塞技术标识（agent_id/SQLite/frontmatter 等放 frontmatter 或正文）。
- 同一日期同一 AI 多份产出末尾标 （1）（2）…（N），（N）前无连字符。
- 目录名必须名副其实（"工具与环境"看不懂→"工具/"）；专属工具加（xx专用）括号。
- **AI 名 = 用户给的角色昵称**（如「记忆知识库管理」），不是模型名/工具名；昵称登记表一文件；不确定就问用户"我叫什么名字"。

## 联动同步铁律（不可谈判）

任何文件/路径/结构改动，必须**立即**全库同步所有相关引用（登记册/索引/说明书/INDEX/交叉引用），**不允许问用户"要不要更新"——联动同步是义务不是选项**。体检脚本 grep 验证。

## 身份与证据纪律

- 文档必写 `创建人：<AI名> <日期>`；修改追加 `修改人：<AI名> <日期>` 行（每次修改一行）；或同时维护 created_by/last_updated_by 字段。
- **归属按会话证据，不凭感觉**：说"某操作是我做的"前先查会话数据库的 session 归属。"文件创建人是 X"是事实表述；"我创建的"需要会话证据。
- **声称"已落地/已完成"必须附可验证证据**（git 提交号/文件路径/脚本输出）——适用于任何任务、任何回复。
- **全量转录**：用户原话、AI 回复、子 agent 对话全部转录入原始凭证档案——只增量追加、绝不覆盖重建；执行正式任务前先把用户刚发的话写入，再检查前几轮缺口追溯补全。

## 敏感信息规矩（判断权在用户）

API Key/Token/密码/客户资料**默认不写入**。遇疑似敏感内容必须停下问用户，告知写入与不写入各自的优劣，用户明确同意才写；**用户不回复 = 反复问直到确切回复**，不擅自继续。AI 不自行判断是否敏感（AI 易双向误判：把示例当真、把真的当无害）。

## 审计不可变

审计报告**一律不改**（不止历史几份）；进行中审计如需非实质性修改，先与用户商议。声称落地必验适用于任何任务：任何"已落地"声称写进汇报前必须程序化扫描验证。

## 知识库健康检查（定期）

- 目录分工是否被遵守（程序化扫描落位）
- 命名日期位置（脚本检查）
- 引用同步（旧名/旧路径残留 grep）
- frontmatter 完整性
- 凭证转录覆盖率（state.db 今日消息签名 vs 转录档案 ≥80%）
- 规矩本身是否过时（定期与用户核对）

## 配套

- 强制层：`ai-rule-enforcement`（体检脚本 + pre-commit 红绿灯 + 注入层设计）
- 转录层：`conversation-raw-log` / `session-transcription`（对话凭证转录）
- 本仓为通用版——具体目录名/豁免名单按你的知识库实际调整（脚本中 EXEMPT_* 配置）。

## 使用示例（Example usage）

**场景**：搭建多 AI 共享知识库的治理。
**执行**：
1. 按目录契约建目录：收件箱/ai暂存/待办/归档/领域知识/工作流/工具/项目
2. 命名规矩入必读卡：<AI名>-<事项>-<日期>.md（日期在末尾）
3. 安装体检脚本 + 提交前钩子（配合 ai-rule-enforcement）
4. 日常流转：产出 → 收件箱 → 验收 → 归位（来源文件回写"✅ 已转交"）
5. 每月健康检查：落位/命名/同步/frontmatter/凭证覆盖率

**输出**：一套机器检查的共享知识库——多 AI 可并行写入不打架。
